To create the base directories for a typical AS3 project run the following at the command line. This build file will take parameters from build.properties and create the base directories for you. Make sure to change the values in the top of build.properties to match your personal settings.

ant -buildfile projinit.xml